<h1><%= t ".title" %></h1>

<ul id="products">
  <% @products.each do |product| %>
    <li><%= link_to product.name, product %></li>
  <% end %>
</ul>

<%= link_to t("new_product"), new_product_path if authenticated? %>
